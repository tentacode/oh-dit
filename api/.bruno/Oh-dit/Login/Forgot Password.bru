meta {
  name: Forgot Password
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/api/forgot_password
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "darth_vader@tentacode.dev"
  }
}

script:post-response {
  const responseJson = res.getBody();
  bru.setEnvVar("AUTH_TOKEN", responseJson.token);
}

settings {
  encodeUrl: false
  timeout: 0
}
