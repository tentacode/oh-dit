import { MdxLayout } from "../../components/layouts/MdxLayout"

# Mon profil

Un formulaire pour gérer les informations de votre compte sera bientôt disponible ici. Entre temps si vous avez besoin de modifier votre email ou votre mot de passe, veuillez contacter le support à l'adresse suivante : [support@ohdit.com](mailto:support@ohdit.com).

[Me déconnecter d'Ohdit](/logout)

export default function Profile({ children }) {
  return <MdxLayout>{children}</MdxLayout>
}