import { MdxLayout } from "../../components/layouts/MdxLayout"
import WorkInProgress from "../../features/work_in_progress/components/WorkInProgress"
import Link from "next/link";
import { ArrowRightStartOnRectangleIcon } from "@heroicons/react/24/outline";
export const metadata = {
  title: "Mon profil - Ohdit",
};

Cette page est en **cours de développement**.

Un formulaire pour gérer les informations de votre compte sera bientôt disponible ici : votre email, votre nom d'utilisateur et votre mot de passe.

Entre-temps, si vous avez besoin de modifier votre email ou votre mot de passe, ou même de supprimer votre compte, vous pouvez contacter le support à l'adresse suivante : [support@ohdit.com](mailto:support@ohdit.com).

<ArrowRightStartOnRectangleIcon style={{
    'display': "inline-block",
    "width": "30px",
    "height": "30px",
    "verticalAlign": "middle",
    "marginTop": "-3px",
}} aria-hidden="true" /> [Je veux me déconnecter d'Ohdit](/logout)

export default function Contact({ children }) {
  return <MdxLayout>  
    <WorkInProgress>
      <div className="mt-8">
        <h1 className="h1">Mon profil</h1>
        {children}
      </div>
    </WorkInProgress>  
  </MdxLayout>
}